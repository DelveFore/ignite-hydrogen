import * as React from 'react'
import { ViewStyle } from 'react-native'
import { View, connectStyle } from 'native-base'

export interface <%= props.pascalName %>Props {
  /**
   * An optional style override useful for padding & margin.
   */
  style?: ViewStyle
}

/**
 * <%= props.description %>
 */
export default (props: <%= props.pascalName %>Props) => {
  return (
    <View />
  )
}
// Connectstyle is expecting a fully qualified name, NOT a function. Need advisement
export default connectStyle ()
