import * as React from 'react'
import { ViewStyle } from 'react-native'
import { View, connectStyle } from 'native-base'

export interface <%= props.pascalName %>Props {
  /**
   * An optional style override useful for padding & margin.
   */
  style?: ViewStyle
}

/**
 * <%= props.description %>
 */
const Component = (props: <%= props.pascalName %>Props) => {
  return (
    <View />
  )
}

export default connectStyle ('custom.<%= props.pascalName %>', {})(Component)
